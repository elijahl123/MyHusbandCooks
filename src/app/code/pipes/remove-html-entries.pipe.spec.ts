import { RemoveHtmlEntriesPipe } from './remove-html-entries.pipe';

describe('RemoveHtmlEntriesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveHtmlEntriesPipe();
    expect(pipe).toBeTruthy();
  });
});
